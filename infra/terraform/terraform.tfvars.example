# Copy to terraform.tfvars (gitignored) and fill in.
project_id  = "REPLACE_WITH_PROJECT_ID"
region      = "me-central1" # or "me-central2" if CNTXT/invoiced-billing onboarded
github_repo = "murad1981/self-service"
# envs, prefix, secret_names, kms_keys have sensible defaults in variables.tf
